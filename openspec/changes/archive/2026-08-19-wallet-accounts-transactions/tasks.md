## 1. Foundation (schema, module scaffold, seed helpers)

- [x] 1.1 Write migration `<timestamp>_create-wallet-tables.sql` creating `wallet_account_type` enum (`checking`, `savings`, `credit_card`, `investment`, `cash`), `wallet_transaction_type` enum (`income`, `expense`), and tables `wallet_accounts` (`id, name, type, balance_cents, created_by, created_at, updated_at`) and `wallet_transactions` (`id, account_id, type, amount_cents, category, description, transaction_date, created_by, created_at`), with `wallet_transactions.category` typed as the existing `reimbursement_category` enum (defined in `20260531160000_create-reimbursements-tables.sql`) — do not create a new category enum. Add indexes on `wallet_transactions(account_id)` and `wallet_transactions(transaction_date DESC)`.
- [x] 1.2 Apply the migration locally and confirm it applies cleanly and FKs resolve (`supabase db reset` or the project's equivalent migration command). CONFIRMED via `npm run test` on 2026-08-19: `supabase db reset` applied `20260805000000_create-wallet-tables.sql` cleanly.
- [x] 1.3 Scaffold `src/modules/wallet/` (`wallet.module.ts`, `wallet-accounts.controller.ts`, `wallet-accounts.service.ts`, `wallet-transactions.controller.ts`, `wallet-transactions.service.ts`, `dto/account.dto.ts`, `dto/account.response.dto.ts`, `dto/transaction.dto.ts`, `dto/transaction.response.dto.ts`) with empty controllers/services, and register `WalletModule` in `app.module.ts`.
- [x] 1.4 Add `RoutePolicy` constants in the wallet controllers per `design.md` Decision 1: `PRESIDENTE_ACCESS` (`rba: [['role', ['presidente']]]`, for rank > 3 writes) and `DIRECTOR_UP_ACCESS` (`rba: [['role', ['diretor', 'assessor', 'presidente']]]`, for rank >= 2 reads).
- [x] 1.5 Add test seed helpers to `src/test/orchestrator.ts`: `createWalletAccount({ name?, type?, balance_cents? })` and `createWalletTransaction({ account_id, type?, amount_cents?, category?, description?, transaction_date? })`, following the existing `createReimbursement` pattern (direct SQL insert via `getPool().query`, `RETURNING *`). Export both from the `seed` object.

## 2. Wallet accounts — create, edit, list

- [x] 2.1 (RED) Create `src/test/wallet/accounts/POST.spec.ts`, `PATCH.spec.ts`, and `GET.spec.ts` covering every scenario in `specs/wallet-accounts/spec.md`, using the `./claude/skills/integration-test` skill's role-based pattern. Run `npm test` and confirm these fail (endpoints don't exist yet).
- [x] 2.2 (GREEN) Implement `CreateWalletAccountDto` (`name`, `type`, optional `balance_cents` defaulting to `0`), `UpdateWalletAccountDto` (`name?`, `type?`), `WalletAccountResponseDto`, and `WalletAccountsService.create/update/findAll/findById`. Wire up `POST /wallet/accounts` and `PATCH /wallet/accounts/:id` with `PRESIDENTE_ACCESS`, and `GET /wallet/accounts` / `GET /wallet/accounts/:id` with `DIRECTOR_UP_ACCESS`.
- [x] 2.3 Run `npm test` and confirm the tests from 2.1 now pass. CONFIRMED via full `npm run test` run on 2026-08-19 (903/903 tests passing, including `src/test/wallet/accounts/*.spec.ts`).
- [x] 2.4 Update `API.md` / OpenAPI docs for `POST /wallet/accounts`, `PATCH /wallet/accounts/:id`, `GET /wallet/accounts`, `GET /wallet/accounts/:id`.

### Testing
- File: `src/test/wallet/accounts/POST.spec.ts` — PRESIDENTE success with default `balance_cents = 0` (201), PRESIDENTE success with explicit `balance_cents` (201), missing `name`/`type` (400), invalid `type` (400), DIRETOR/ASSESSOR/GERENTE/CONSULTOR forbidden (403), unauthenticated (401).
- File: `src/test/wallet/accounts/PATCH.spec.ts` — PRESIDENTE updates `name` and/or `type` (200), invalid `type` (400), non-PRESIDENTE forbidden (403), unknown account id (404).
- File: `src/test/wallet/accounts/GET.spec.ts` — DIRETOR/ASSESSOR/PRESIDENTE list all accounts with correct `balance_cents` (200), DIRETOR/ASSESSOR/PRESIDENTE fetch single account (200), unknown account id (404), GERENTE/CONSULTOR forbidden on list and single-fetch (403), unauthenticated (401).

## 3. Wallet transactions — create, list

- [x] 3.1 (RED) Create `src/test/wallet/transactions/POST.spec.ts` and `GET.spec.ts` covering every scenario in `specs/wallet-transactions/spec.md`, using the `./claude/skills/integration-test` skill. Run `npm test` and confirm these fail (endpoints don't exist yet).
- [x] 3.2 (GREEN) Implement `CreateWalletTransactionDto` (`account_id`, `type` enum `income`/`expense`, `amount_cents` positive int, `category` reusing the `REIMBURSEMENT_CATEGORIES` values from `reimbursements/dto/reimbursement.dto.ts`, `description`, `transaction_date`), `WalletTransactionResponseDto`, and `WalletTransactionsService.create/findAll`. `create` MUST run inside `DatabaseService.withTransaction` per `design.md` Decision 3: verify the account exists (404 if not), insert the `wallet_transactions` row, and `UPDATE wallet_accounts SET balance_cents = balance_cents + $delta WHERE id = $1` in the same DB transaction, where `$delta` is `+amount_cents` for `income` and `-amount_cents` for `expense`. No balance-sufficiency check. Wire up `POST /wallet/transactions` with `PRESIDENTE_ACCESS`, `GET /wallet/transactions` (with optional `account_id` query filter) with `DIRECTOR_UP_ACCESS`.
- [x] 3.3 Run `npm test` and confirm the tests from 3.1 now pass. CONFIRMED via full `npm run test` run on 2026-08-19 — required fixing `CreateWalletTransactionDto.account_id` from `z.string().uuid()` to `z.string().guid()` so the non-existent-account test (404) isn't rejected as a 400 by Zod's strict versioned-UUID check; 903/903 tests passing after the fix, including `src/test/wallet/transactions/POST.spec.ts`.
- [x] 3.4 Update `API.md` / OpenAPI docs for `POST /wallet/transactions` and `GET /wallet/transactions` (including the `account_id` query param).

### Testing
- File: `src/test/wallet/transactions/POST.spec.ts` — PRESIDENTE creates an `expense` and the account's `balance_cents` decreases by `amount_cents` (201), PRESIDENTE creates an `income` and the account's `balance_cents` increases (201), `expense` larger than current balance still succeeds and balance goes negative (201), unknown `account_id` (404, no row persisted), invalid `category` (400), invalid `type` (400), `amount_cents` zero/negative (400), missing required fields (400), non-PRESIDENTE forbidden (403), unauthenticated (401).
- File: `src/test/wallet/transactions/GET.spec.ts` — DIRETOR/ASSESSOR/PRESIDENTE list all transactions (200), filter by `account_id` returns only that account's transactions (200), GERENTE/CONSULTOR forbidden (403), unauthenticated (401).

## 4. Reimbursement approval creates a wallet transaction (with optional partial payout)

- [x] 4.1 (RED) Extend/create `src/test/reinbursements/PATCH.spec.ts` (status endpoint — note: the existing test directory is spelled `reinbursements`, not `reimbursements`; followed the codebase's existing typo instead of introducing a second, correctly-spelled directory) covering the modified scenarios in `specs/reimbursements-status/spec.md`: full approval with `account_id` debits the account and creates a transaction for `amount_cents`, partial approval with `paid_amount_cents` + `partial_reason` debits only `paid_amount_cents` and persists both fields, partial approval without `partial_reason` (400), `paid_amount_cents` greater than `amount_cents` (400), `paid_amount_cents` zero/negative (400), approval without `account_id` (400), approval with unknown `account_id` (404, status unchanged), rejection unaffected (no `account_id`/`paid_amount_cents`/`partial_reason` needed, no transaction created). Docker is unavailable in this environment, so RED could not be confirmed by actually running `npm test`; verified statically instead (`tsc --noEmit`, `oxlint --deny-warnings`, `docs:check` all pass) — run `npm test` to confirm RED→GREEN for real before merging.
- [x] 4.1a Write migration `<timestamp>_add-partial-approval-to-reimbursements.sql` adding `paid_amount_cents INTEGER` and `partial_reason TEXT` (both nullable, no default) to `reimbursements`. Written as `20260805010000_add-partial-approval-to-reimbursements.sql`; NOT applied/verified locally (supabase CLI is installed but docker, which the local Postgres stack needs, is unavailable in this environment) — run `supabase db reset` to confirm it applies cleanly.
- [x] 4.2 (GREEN) Update `UpdateReimbursementStatusDto` in `reimbursements/dto/reimbursement.dto.ts` to add `account_id: z.string().uuid().optional()`, `paid_amount_cents: z.number().int().positive().optional()`, and `partial_reason: z.string().min(1).optional()`, with a `.refine()` requiring `account_id` when `status === 'approved'`. Update `ReimbursementRow`/`ReimbursementResponseDto` to include `paid_amount_cents: number | null` and `partial_reason: string | null`. Update `ReimbursementsService.updateStatus` per `design.md` Decision "Partial approval": wrap the existing status UPDATE in `DatabaseService.withTransaction`; when approving, compute `payout = dto.paid_amount_cents ?? reimbursement.amount_cents`, validate `0 < payout <= reimbursement.amount_cents` (400 otherwise) and require `partial_reason` when `payout < reimbursement.amount_cents` (400 if missing), verify `account_id` resolves to an existing `wallet_accounts` row (404 if not), insert a `wallet_transactions` row (`type: 'expense'`, `amount_cents = payout`, `category` = the reimbursement's own `category`, `description` referencing the reimbursement id/title and appending `(parcial)` when `payout < reimbursement.amount_cents`), set `paid_amount_cents = payout` and `partial_reason = dto.partial_reason ?? null` on the reimbursement, and apply the balance delta to the account — all inside the same transaction so a failure rolls back the status change too.
- [x] 4.3 Run `npm test` and confirm the tests from 4.1 now pass, and confirm no existing reimbursement tests regressed. CONFIRMED via full `npm run test` run on 2026-08-19 — required the same `account_id` fix as 3.3 (`z.string().uuid()` → `.guid()` in `UpdateReimbursementStatusDto`) for the unknown-`account_id` 404 case; no regressions, 903/903 tests passing.
- [x] 4.4 Update `API.md` / OpenAPI docs for `PATCH /reimbursements/:id/status` to document the new conditionally-required `account_id` field and the optional `paid_amount_cents`/`partial_reason` partial-approval fields.

### Testing
- File: `src/test/reimbursements/PATCH.spec.ts` — PRESIDENTE approves in full with a valid `account_id` (200, reimbursement `status = 'approved'`, `paid_amount_cents === amount_cents`, account `balance_cents` decreased by `amount_cents`, a `wallet_transactions` row of `type = 'expense'` exists for that account/amount/category), PRESIDENTE partially approves with `paid_amount_cents` + `partial_reason` (200, `paid_amount_cents` and `partial_reason` persisted, account `balance_cents` decreased by `paid_amount_cents` only, wallet transaction amount equals `paid_amount_cents`), partial approval without `partial_reason` (400, status stays `pending`, no transaction row created), `paid_amount_cents` greater than `amount_cents` (400), `paid_amount_cents` zero or negative (400), approve without `account_id` (400, status stays `pending`), approve with unknown `account_id` (404, status stays `pending`, no transaction row created), PRESIDENTE rejects without `account_id`/`paid_amount_cents`/`partial_reason` (200, `status = 'rejected'`, `paid_amount_cents`/`partial_reason` remain `null`, no transaction row created), re-resolve an already-resolved reimbursement (400), invalid `status` value (400), `status = 'pending'` (400), non-PRESIDENTE forbidden (403), unknown reimbursement id (404).

## 5. Finish

- [x] 5.1 Run `rtk lint` (or the project's ESLint command) on all files under `src/modules/wallet/`, `src/modules/reimbursements/`, `src/test/wallet/`, `src/test/reinbursements/`, and `src/test/orchestrator.ts`; fix any violations. Ran `oxlint --deny-warnings src/` (full tree) and `prettier --check`/`--write` on all changed files — both clean.
- [x] 5.2 Run `npm test` for the full suite and confirm everything is green, including all new/changed files from tasks 2–4. CONFIRMED on 2026-08-19: `npm run test` (supabase+docker+nest+jest) — 104/104 suites, 903/903 tests passing.
- [x] 5.3 Confirm `API.md` / OpenAPI now documents all 6 new/changed endpoints: `POST /wallet/accounts`, `PATCH /wallet/accounts/:id`, `GET /wallet/accounts`, `GET /wallet/accounts/:id`, `POST /wallet/transactions`, `GET /wallet/transactions`, plus the updated `PATCH /reimbursements/:id/status`. Confirmed in `API.md` (new `## Wallet` section) and `openapi.json` (regenerated via `npm run docs:generate`, verified via `npm run docs:check`).
